# locallink-backend/.env.example
# Copy this file to .env and replace the placeholder values.

# --- APPLICATION SETTINGS ---
PORT=5000
NODE_ENV=development # Use 'production' in production

# --- DATABASE ---
# Use your MongoDB Atlas connection string (recommended) or a local URI.
# Ensure you replace <username> and <password> with your DB credentials
MONGO_URI=mongodb+srv://<username>:<password>@clustername.abcde.mongodb.net/locallink_db?retryWrites=true&w=majority

# --- SECURITY ---
# A long, random string. Must be kept secret.
JWT_SECRET=YOUR_VERY_STRONG_SECRET_KEY_HERE
# Token expiry time (e.g., '1h', '7d', '15m')
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=ANOTHER_VERY_STRONG_SECRET_KEY
JWT_REFRESH_EXPIRES_IN=30d

# --- CLOUDINARY (Image Uploads) ---
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# --- EMAIL SERVICE (Nodemailer - Example with Mailtrap/SendGrid) ---
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="LocalLink Support <support@locallink.com>"